# pathVisualiser
A dynamic path visualization web app that lets users add cities and paths, then animate the shortest route between any two cities using Dijkstra's Algorithm. Built using JavaScript and HTML Canvas with real-time animation and interactivity.
